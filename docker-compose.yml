services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: bilibili
      POSTGRES_USER: bilibili
      POSTGRES_PASSWORD: bilibili-dev-only
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bilibili -d bilibili"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build: .
    restart: unless-stopped
    env_file: .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8100
    ports:
      - "8100:8100"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - work_cache:/tmp/bilibili-processor
      - storage_data:/var/lib/bilibili-processor/storage

  worker:
    build: .
    restart: unless-stopped
    env_file: .env
    command: python -m app.worker
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - work_cache:/tmp/bilibili-processor
      - storage_data:/var/lib/bilibili-processor/storage

volumes:
  postgres_data:
  work_cache:
  storage_data:
